import React from "react";

const ListaHeading = (props) => {
    return (
        <div className="col">
            <h1 className="lista-heading">{props.heading}</h1>
        </div>
    )
}

export default ListaHeading; 